---
name: grill-with-docs
description: 既存のドメインモデルに対してプランを検証し、用語を研ぎ澄ませ、決定が固まる都度ドキュメント（CONTEXT.md、ADR）をインラインで更新するグリルセッション。プロジェクトの言語と記録済み決定に照らしてプランをストレステストしたいときに使う。
---

<what-to-do>

このプランのあらゆる側面について、共通理解に到達するまで容赦なくインタビューする。設計ツリーの各枝を下り、決定間の依存関係を一つずつ解消する。各質問に対して推奨回答も提示する。

質問は一度に一つずつ行い、次の質問に進む前に各回答へのフィードバックを待つ。

コードベースを探索することで答えられる質問は、コードベースを探索する。

</what-to-do>

<supporting-info>

## ドメイン認識

コードベース探索時に既存ドキュメントも確認する:

### ファイル構造

ほとんどのリポジトリは単一コンテキスト:

```
/
├── CONTEXT.md
├── docs/
│   └── adr/
│       ├── 0001-event-sourced-orders.md
│       └── 0002-postgres-for-write-model.md
└── src/
```

ルートに `CONTEXT-MAP.md` が存在する場合、リポジトリは複数コンテキストを持つ。マップは各コンテキストの所在を示す:

```
/
├── CONTEXT-MAP.md
├── docs/
│   └── adr/                          ← システム全体の決定
├── src/
│   ├── ordering/
│   │   ├── CONTEXT.md
│   │   └── docs/adr/                 ← コンテキスト固有の決定
│   └── billing/
│       ├── CONTEXT.md
│       └── docs/adr/
```

ファイルは遅延生成 — 書くものができてから作る。`CONTEXT.md` が存在しない場合、最初の用語が確定したときに作成する。`docs/adr/` が存在しない場合、最初のADRが必要になったときに作成する。

## セッション中

### 用語集との照合

ユーザーが `CONTEXT.md` の既存の言語と矛盾する用語を使った場合、すぐに指摘する。「用語集では『キャンセル』をXと定義しているが、あなたはYを意味しているようだ — どちらが正しいか？」

### 曖昧な言語の精緻化

ユーザーが曖昧または多義的な用語を使った場合、正確な標準用語を提案する。「『アカウント』と言っているが、CustomerのことかUserのことか？それらは別物だ。」

### 具体的なシナリオでの議論

ドメインの関係性を議論するとき、具体的なシナリオでストレステストする。エッジケースを探り、概念の境界についてユーザーに正確さを求めるシナリオを作る。

### コードとの照合

ユーザーが何かの動作を説明したとき、コードがそれに同意しているか確認する。矛盾を発見したら指摘する:「コードは注文全体をキャンセルしているが、あなたは部分キャンセルが可能と言った — どちらが正しいか？」

### CONTEXT.md のインライン更新

用語が確定したら、その場で `CONTEXT.md` を更新する。まとめて後回しにしない — 発生した瞬間に記録する。[CONTEXT-FORMAT.md](./CONTEXT-FORMAT.md) のフォーマットを使う。

`CONTEXT.md` は実装の詳細を一切含まない。`CONTEXT.md` を仕様書、メモ帳、実装上の決定のリポジトリとして扱わない。用語集であり、それ以外の何物でもない。

### ADR は慎重に提案する

以下の三つすべてが真のときのみADR作成を提案する:

1. **覆しにくい** — 後で考えを変えるコストが大きい
2. **文脈なしでは驚かれる** — 将来の読者が「なぜこうしたのか？」と疑問を持つ
3. **真のトレードオフの結果** — 真剣な代替案が存在し、特定の理由で一つを選んだ

三つのうち一つでも欠けていればADRはスキップする。[ADR-FORMAT.md](./ADR-FORMAT.md) のフォーマットを使う。

</supporting-info>
