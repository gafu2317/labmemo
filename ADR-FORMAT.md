# ADR フォーマット

ADRは `docs/adr/` に置き、連番で命名する: `0001-slug.md`、`0002-slug.md` など。

`docs/adr/` ディレクトリは遅延作成 — 最初のADRが必要になったときのみ作る。

## テンプレート

```md
# {決定の短いタイトル}

{1〜3文: コンテキスト、何を決定したか、なぜ。}
```

以上。ADRは1段落で十分。価値は決定が行われたこと と その理由を記録することにあり、セクションを埋めることにはない。

## オプションセクション

真に価値を加えるときのみ含める。ほとんどのADRには不要。

- **ステータス** フロントマター（`proposed | accepted | deprecated | superseded by ADR-NNNN`）— 決定が見直されるときに有用
- **検討した選択肢** — 却下した代替案を記憶に値するときのみ
- **結果** — 非自明な下流への影響を明示する必要があるときのみ

## 採番

`docs/adr/` の最大番号をスキャンして1加算する。

## ADR を提案するタイミング

以下の三つすべてが真でなければならない:

1. **覆しにくい** — 後で考えを変えるコストが大きい
2. **文脈なしでは驚かれる** — 将来の読者がコードを見て「なぜこんな実装にしたのか」と疑問を持つ
3. **真のトレードオフの結果** — 真剣な代替案が存在し、特定の理由で一つを選んだ

決定が覆しやすければスキップする — 必要なら覆せばいい。驚かれないなら誰も疑問を持たない。真の代替案がなければ「明らかな選択をした」以上に記録することはない。

### 対象となるもの

- **アーキテクチャの形状。** 「モノレポを使う。」「書き込みモデルはイベントソーシング、読み込みモデルはPostgresに投影する。」
- **コンテキスト間の統合パターン。** 「注文と請求はドメインイベントで通信し、同期HTTPは使わない。」
- **ロックインを伴う技術選択。** DB、メッセージバス、authプロバイダー、デプロイ先。すべてのライブラリではなく、入れ替えに四半期かかるようなものだけ。
- **境界とスコープの決定。** 「顧客データは顧客コンテキストが所有し、他のコンテキストはIDでのみ参照する。」明示的なNoはYesと同様に価値がある。
- **明らかな選択からの意図的な逸脱。** 「Xのためにつを使わずに生SQLを使う。」合理的な読者が反対のことを想定するような箇所。これにより次のエンジニアが意図的な選択を「修正」することを防ぐ。
- **コードに見えない制約。** 「コンプライアンス要件のためAWSは使えない。」「パートナーAPIの契約によりレスポンスは200ms以内でなければならない。」
- **却下が非自明な代替案。** GraphQLを検討してREST を選んだ微妙な理由があるなら記録する — そうしなければ6ヶ月後にまたGraphQLを提案される。
