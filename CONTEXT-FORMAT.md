# CONTEXT.md フォーマット

## 構造

```md
# {コンテキスト名}

{このコンテキストが何であり、なぜ存在するかを1〜2文で説明。}

## 言語

**注文**:
{用語の説明を1〜2文で}
_使わない_: 購入、トランザクション

**請求書**:
納品後に顧客へ送る支払い要求。
_使わない_: 請求、支払い依頼

**顧客**:
注文を行う個人または組織。
_使わない_: クライアント、購買者、アカウント
```

## ルール

- **意見を持つ。** 同じ概念に複数の言葉が存在する場合、最良の一つを選び、他を `_使わない_` に列挙する。
- **定義を簡潔に。** 最大1〜2文。それが「何であるか」を定義し、「何をするか」は書かない。
- **このプロジェクトのコンテキスト固有の用語のみ含める。** タイムアウト、エラー型、ユーティリティパターンなど一般的なプログラミング概念は、プロジェクトで多用していても含めない。用語を追加する前に問う: これはこのコンテキスト固有の概念か、一般的なプログラミング概念か？前者のみ含める。
- **自然なクラスターが生まれたら小見出しでグループ化する。** すべての用語が一つの凝集したエリアに属する場合、フラットリストでよい。

## 単一コンテキスト vs 複数コンテキストのリポジトリ

**単一コンテキスト（ほとんどのリポジトリ）:** リポジトリルートに `CONTEXT.md` を一つ。

**複数コンテキスト:** リポジトリルートの `CONTEXT-MAP.md` がコンテキスト、その所在、相互関係を列挙する:

```md
# コンテキストマップ

## コンテキスト

- [注文](./src/ordering/CONTEXT.md) — 顧客注文を受け付け追跡する
- [請求](./src/billing/CONTEXT.md) — 請求書を生成し支払いを処理する
- [フルフィルメント](./src/fulfillment/CONTEXT.md) — 倉庫のピッキングと出荷を管理する

## 関係性

- **注文 → フルフィルメント**: 注文が `OrderPlaced` イベントを発行；フルフィルメントがピッキング開始のためにそれを消費する
- **フルフィルメント → 請求**: フルフィルメントが `ShipmentDispatched` イベントを発行；請求が請求書生成のためにそれを消費する
- **注文 ↔ 請求**: `CustomerId` と `Money` の共有型
```

スキルはどちらの構造が適用されるかを推論する:

- `CONTEXT-MAP.md` が存在すれば、それを読んでコンテキストを特定する
- ルートの `CONTEXT.md` のみ存在すれば、単一コンテキスト
- どちらも存在しなければ、最初の用語が確定したときにルートの `CONTEXT.md` を遅延作成する

複数コンテキストが存在する場合、現在のトピックがどれに関連するかを推論する。不明な場合は確認する。
